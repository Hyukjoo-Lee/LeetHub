class Solution {
    public boolean divisorGame(int n) {
        /**
            Alice
            Bob
            chalkboard??
            0 < x < n 
            n % x == 0
            2 % 2 == 0
            3 % 1 == 0
            cannot make a move, lose the game


         */

         return n % 2 == 0;
    }
}